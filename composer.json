{
    "name": "live-event-manager/wordpress-plugin",
    "description": "WordPress plugin for managing live stream events and JWT generation",
    "type": "wordpress-plugin",
    "require": {
        "firebase/php-jwt": "^6.0"
    },
    "autoload": {
        "psr-4": {
            "LiveEventManager\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
} 